<%- include('./includes/head.ejs') %>
<div class="container h-100">
    <div class="row h-100 justify-content-center align-items-center">
        <div class="col-10 col-md-8 col-lg-6">
            <!-- Form -->
            <%if(errors.length > 0) {%>
            <div class="alert alert-dismissible alert-danger">
                <strong><%=errors%></strong>
            </div>
            <%}%>
            <form class="form-example" action="/resetPassword" method="post">
                <h1>Restablecer contraseña</h1>
                <!-- Input fields -->
                <div class="form-group">
                    <label for="username">Email:</label>
                    <input type="text" class="form-control username" id="email" placeholder="test@test.com" name="email">
                </div>
                <button type="submit" class="btn btn-primary btn-customized">Login</button>
                <!-- End input fields -->
            </form>
            <!-- Form end -->
        </div>
    </div>
</div>
<%- include('includes/end.ejs')%>
